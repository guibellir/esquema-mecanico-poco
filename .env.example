# Desenvolvimento local: proxy do Vite (recomendado — evita CORS)
VITE_API_URL=/cloud-api
VITE_API_PROXY_TARGET=https://scan-esquema-poco.evu7va.easypanel.host

# Produção / se preferir chamar a API direto (precisa CORS liberado):
# VITE_API_URL=https://scan-esquema-poco.evu7va.easypanel.host
